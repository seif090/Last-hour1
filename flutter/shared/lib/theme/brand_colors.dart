import 'package:flutter/material.dart';

/// Brand colors for Last Hour — Electric Crimson + Deep Navy palette.
class BrandColors {
  BrandColors._();

  // ── Primary ──
  static const Color primary = Color(0xFFE94560);
  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color primaryContainer = Color(0xFFFC536D);
  static const Color onPrimaryContainer = Color(0xFF40000E);

  // ── Surface & Background — dark theme ──
  static const Color surface = Color(0xFF1A1A2E);
  static const Color surfaceDim = Color(0xFF151528);
  static const Color surfaceBright = Color(0xFF2A2A44);
  static const Color surfaceContainerLowest = Color(0xFF121225);
  static const Color surfaceContainerLow = Color(0xFF1D1D34);
  static const Color surfaceContainer = Color(0xFF22223B);
  static const Color surfaceContainerHigh = Color(0xFF2D2D48);
  static const Color surfaceContainerHighest = Color(0xFF383855);

  static const Color onSurface = Color(0xFFF1F1F9);
  static const Color onSurfaceVariant = Color(0xFF94A3B8);

  // ── Background ──
  static const Color background = Color(0xFF1A1A2E);
  static const Color onBackground = Color(0xFFF1F1F9);

  // ── Secondary ──
  static const Color secondary = Color(0xFFBBC5EB);
  static const Color onSecondary = Color(0xFF252F4D);
  static const Color secondaryContainer = Color(0xFF3B4665);

  // ── Tertiary ──
  static const Color tertiary = Color(0xFF67DC9F);
  static const Color onTertiary = Color(0xFF003921);

  // ── Error ──
  static const Color error = Color(0xFFFFB4AB);
  static const Color onError = Color(0xFF690005);

  // ── Outline ──
  static const Color outline = Color(0x14FFFFFF);
  static const Color outlineVariant = Color(0x2EFFFFFF);

  // ── Light-theme overrides ──
  static const Color lightSurface = Color(0xFFF8F9FA);
  static const Color lightOnSurface = Color(0xFF1A1A2E);
  static const Color lightOnSurfaceVariant = Color(0xFF64748B);
  static const Color lightOutline = Color(0xFFE2E8F0);
}
