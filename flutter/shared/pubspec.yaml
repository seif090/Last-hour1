name: lasthour_shared
description: Last Hour — Shared models, enums, constants, and theme
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5
  connectivity_plus: ^6.0.0
  google_fonts: ^6.1.0
